pipeline {
    agent any
    // triggers {
    //     pollSCM('H */ * * 1-5')
    // }
    stages {
        stage('build') {
            steps {
                sh "echo something"
                sh "docker build -t api_test_base . -f Dockerfile_base"
            }
        }
    }
}